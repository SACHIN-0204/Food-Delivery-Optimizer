server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # React Router uses client-side routing — any unknown path should still
    # serve index.html so the app can handle it, instead of nginx 404ing.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
